export const HTML = "<header class=\"help-page-header\">\r\n    <h1>Intégration WPF</h1>\r\n    <p class=\"lead\">\r\n      WPF utilise le package et les API <strong>LicenPro</strong> identiques à WinForms. LicenPro ne livre pas de SDK WPF dédié ni de <code>ILicenseService</code> — vous encapsulez <code>LicenseClient</code> dans votre propre service enregistré en DI, le gardez actif pour la durée du processus, et acheminez les événements vers le thread UI si nécessaire.\r\n    </p>\r\n  </header>\r\n\r\n  <h2>1. Démarrage application</h2>\r\n  <p>Appelez bootstrap avant d&apos;afficher la fenêtre principale pour que les mises à jour staged et le chargement JSON se comportent exactement comme WinForms.</p>\r\n\r\n  <pre><code>using System.Windows;\r\nusing LicenPro.SDK.AppHosting;\r\n\r\npublic partial class App : Application\r\n{{ '{' }}\r\n    protected override void OnStartup(StartupEventArgs e)\r\n    {{ '{' }}\r\n        SdkBootstrap.OnApplicationStartup();\r\n        base.OnStartup(e);\r\n    {{ '}' }}\r\n{{ '}' }}</code></pre>\r\n\r\n  <h2>2. Validation async sans bloquer le thread UI</h2>\r\n  <p>\r\n    Construisez <code>LicenseClient</code> sur un thread background ou le thread UI, mais <code>await</code> toujours la validation.\r\n    <code>LicenseClient</code> capture <code>SynchronizationContext.Current</code> à la création ; les avertissements d&apos;expiration (<code>LicenseExpiringSoon</code>) sont renvoyés sur ce contexte, donc créer le client sur le thread UI garde les dialogues sûrs.\r\n  </p>\r\n\r\n  <h2>3. Couche orientée MVVM</h2>\r\n  <p>Forme recommandée :</p>\r\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\r\n    <li><strong>ILicenseService</strong> (votre interface) encapsule <code>LicenseClient</code>, expose validate / try-auto-validate basés sur <code>Task</code>, et transmet <code>SessionDisconnected</code>.</li>\r\n    <li><strong>Hôte niveau app</strong> possède la durée de vie du service et appelle <code>DisposeAsync</code> à l&apos;arrêt.</li>\r\n    <li><strong>View models</strong> lient des commandes au service ; ne bloquez jamais le thread UI avec des API de validation synchrones.</li>\r\n  </ul>\r\n\r\n  <h2>4. Identité matérielle</h2>\r\n  <p>\r\n    Le SDK calcule en interne une empreinte matérielle composite pour activation et vérifications de mise à jour. Vous n&apos;avez normalement pas besoin d&apos;un « WPF hardware provider » séparé ; fournissez <code>IHardwareIdentifier</code> uniquement dans des scénarios de test avancés.\r\n  </p>\r\n\r\n  <h2>5. Pour aller plus loin</h2>\r\n  <div class=\"integration-grid\">\r\n    <a href=\"/sdk/dotnet\" class=\"integration-card\">\r\n      <i class=\"ki-outline ki-code\"></i>\r\n      <h4>.NET SDK</h4>\r\n      <p>Options, cache, mises à jour, sessions</p>\r\n    </a>\r\n    <a href=\"/sdk/winforms\" class=\"integration-card\">\r\n      <i class=\"ki-outline ki-mouse\"></i>\r\n      <h4>Référence WinForms</h4>\r\n      <p>Patterns de cycle de vie parallèles</p>\r\n    </a>\r\n  </div>" as const;
