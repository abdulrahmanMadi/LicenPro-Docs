export const HTML = "<article class=\"help-page\">\n  <header class=\"help-page-header\">\n    <h1>Vue d&apos;ensemble REST API</h1>\n    <p class=\"lead\">\n      LicenPro expose une API ASP.NET Core sous <code>/api/...</code> sur votre déploiement (cloud :\n      <code>https://licenpro.runasp.net/api</code>). La plupart des routes de gestion requièrent un\n      <strong>Bearer JWT</strong> (utilisateurs du tableau de bord). Le SDK et les intégrations serveur anonymes utilisent\n      <strong><code>X-API-KEY</code></strong> sur certaines routes comme <code>POST /api/Licenses/validate</code>.\n    </p>\n  </header>" as const;
