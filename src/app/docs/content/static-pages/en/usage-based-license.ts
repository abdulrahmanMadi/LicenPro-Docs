export const HTML = "<header class=\"help-page-header\">\n    <h1>Usage-Based license</h1>\n    <p class=\"lead\">\n      A <strong>Usage-Based</strong> license (<code>MeteredCount</code>, metering mode <code>Count</code>) is\n      <strong>always online</strong>. Customers share one <strong>use counter</strong> across all devices on the\n      license key. Every consume deducts <strong>quantity</strong> from the remaining uses — there is no per-feature\n      token pricing (unlike <a href=\"/credit-based-license\" class=\"doc-inline-link\">Credit-Based</a>).\n    </p>\n  </header>\n\n  <div class=\"integration-grid\">\n    <div class=\"integration-card\">\n      <i class=\"ki-outline ki-chart-simple\"></i>\n      <h4>Shared counter</h4>\n      <p>One “uses” balance per license key.</p>\n    </div>\n    <div class=\"integration-card\">\n      <i class=\"ki-outline ki-abstract-26\"></i>\n      <h4>Flat deduction</h4>\n      <p>Each consume costs <code>quantity</code> (usually 1).</p>\n    </div>\n    <div class=\"integration-card\">\n      <i class=\"ki-outline ki-chart-line-up\"></i>\n      <h4>Usage tab</h4>\n      <p>Balance, ledger, manual top-ups (owners).</p>\n    </div>\n  </div>\n\n  <nav class=\"guide-audience-nav\" aria-label=\"Guide sections\">\n    <span class=\"guide-audience-nav__label\">Jump to:</span>\n    <a href=\"#product-owner-guide\" class=\"guide-audience-nav__link guide-audience-nav__link--owner\">Product owner guide</a>\n    <a href=\"#end-user-guide\" class=\"guide-audience-nav__link guide-audience-nav__link--user\">End user guide</a>\n  </nav>\n\n  <section id=\"product-owner-guide\" class=\"guide-audience-section guide-audience-section--owner\">\n    <h2>Usage-Based license — for product owners</h2>\n    <ul class=\"guide-checklist\">\n      <li>Issue with <strong>initial use balance</strong> — no per-feature pricing required.</li>\n      <li>Grant or adjust balance from the <strong>Usage</strong> tab or meter API (<code>POST /api/meter/grant</code>, <code>adjust</code>).</li>\n      <li>Review ledger and run reconciliation to verify wallet vs entries.</li>\n      <li>Entitlement sets restrict which feature names can be consumed.</li>\n      <li>Always online — no offline Usage-Based mode.</li>\n    </ul>\n    <p>Detailed wizard and API examples are documented below.</p>\n  </section>\n\n  <section id=\"end-user-guide\" class=\"guide-audience-section guide-audience-section--user\">\n    <h2>Usage-Based license — for end users</h2>\n    <p>\n      Your license includes a fixed number of <strong>uses</strong> shared across your team’s devices.\n      The app remains licensed at zero uses, but metered actions stop with <strong>insufficient balance</strong>.\n    </p>\n    <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n      <li>Each billable action typically costs <strong>1 use</strong> (integrator may pass higher quantity).</li>\n      <li>When uses run out, ask your admin for a top-up or plan upgrade.</li>\n      <li>View remaining uses in-app if the vendor exposed balance UI.</li>\n    </ul>\n  </section>\n\n  <h2>When to choose Usage-Based</h2>\n  <p>\n    Choose <strong>Usage-Based</strong> when you sell a <strong>fixed number of actions</strong> and every billable\n    operation should count the same — typically <strong>one use per call</strong>. The customer starts with a balance\n    (for example 500 uses). Each time they run a metered feature, your app calls <code>ConsumeAsync</code> and the\n    server subtracts 1 use (or more if you pass a higher <code>quantity</code>).\n  </p>\n  <p>\n    Think of it like a punch card: “This license includes 500 exports.” Export #1 uses one punch, export #2 uses one\n    punch — it does not matter which export template they pick; each export costs the same.\n  </p>\n\n  <h3>Good fits</h3>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li><strong>Simple bundled SKUs</strong> — “500 PDF exports per year” or “1,000 API calls included.”</li>\n    <li><strong>Evaluation limits</strong> — “Try 50 full runs before buying a larger pack.”</li>\n    <li><strong>Flat per-event billing</strong> — every report generated, every sync job, every upload counts as one use.</li>\n    <li><strong>Operations that should not need a price table</strong> — you do not want to maintain different credit costs per feature.</li>\n  </ul>\n\n  <h3>How it behaves in LicenPro</h3>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li>Dashboard label: <strong>Usage-Based</strong>. API enum: <code>MeteredCount</code>.</li>\n    <li>Balance is shown in <strong>uses</strong> (shared across all devices on the license key).</li>\n    <li><strong>No per-feature pricing</strong> — the feature name is for entitlement and audit; cost is always <code>quantity</code>.</li>\n    <li>Example: customer has 500 uses → one consume with <code>quantity: 1</code> leaves <strong>499 uses</strong>, regardless of feature name.</li>\n  </ul>\n\n  <h3>When to use Credit-Based instead</h3>\n  <p>\n    If different features should burn the wallet at <strong>different rates</strong> — a light API call costs 1 credit\n    but a heavy AI job costs 50 — use <a href=\"/credit-based-license\" class=\"doc-inline-link\">Credit-Based</a> (<code>MeteredToken</code>)\n    and configure a token price per feature.\n  </p>\n\n  <h2>At a glance</h2>\n  <table class=\"doc-table\">\n    <thead>\n      <tr><th>Property</th><th>Value</th></tr>\n    </thead>\n    <tbody>\n      <tr><td>Backend enum</td><td><code>LicenseType.MeteredCount</code> (value <code>7</code>)</td></tr>\n      <tr><td>Dashboard label</td><td><strong>Usage-Based</strong></td></tr>\n      <tr><td>Metering mode</td><td><code>MeteringMode.Count</code></td></tr>\n      <tr><td>Balance unit</td><td><strong>uses</strong></td></tr>\n      <tr><td>Cost per consume</td><td><code>quantity</code> (1 use per unit by default)</td></tr>\n      <tr><td>Feature pricing</td><td><strong>Not used</strong> — feature name is for entitlement/audit only</td></tr>\n      <tr><td>Activation mode</td><td><strong>Online only</strong></td></tr>\n      <tr><td>Seat limit</td><td>None — wallet/counter is the limit, not device seats</td></tr>\n    </tbody>\n  </table>\n\n  <h2>Key invariant</h2>\n  <p>\n    Balance is <strong>only</strong> checked and deducted in <code>POST /api/meter/consume</code>.\n    Validation and activation succeed even at <strong>zero uses</strong>; your app must call consume when the\n    metered action runs and handle <code>InsufficientBalance</code> (HTTP 402).\n  </p>\n\n  <h2>Create a Usage-Based license (dashboard)</h2>\n  <p>\n    Open <strong>Licenses</strong> → <strong>Generate License</strong>. Four steps:\n    <strong>Activation</strong> → <strong>License Type</strong> → <strong>Basic Info</strong> → <strong>Details</strong>.\n  </p>\n\n  <h3>Step 1 — Activation</h3>\n  <p>Select <strong>Online</strong>. Usage-Based is always-online; offline mode hides metered type cards.</p>\n\n  <h3>Step 2 — License type</h3>\n  <p>Select the <strong>Usage-Based</strong> card (“One use per action”). Description: shared use counter, manual top-ups from the dashboard.</p>\n  <figure class=\"doc-figure doc-figure-card\">\n    <button type=\"button\" class=\"doc-figure-view-link\" data-doc-image-lightbox=\"/assets/docs/platform-create-license-step-license-type.png\" data-doc-image-alt=\"License Type step with Usage-Based selected\" aria-label=\"View License Type step screenshot full size\">\n      <img src=\"assets/docs/platform-create-license-step-license-type.png\" alt=\"License Type step with Usage-Based selected\" loading=\"lazy\" decoding=\"async\" />\n    </button>\n    <figcaption class=\"doc-figure-caption\">License Type — Usage-Based (MeteredCount).</figcaption>\n  </figure>\n\n  <h3>Step 3 — Basic info</h3>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li><strong>Product</strong> and <strong>Software release</strong> (required).</li>\n    <li><strong>Issued to</strong> — end user for the release.</li>\n    <li><strong>License name</strong> — internal label.</li>\n    <li>Optional <strong>Entitlement set</strong> — restricts which feature names may be consumed.</li>\n  </ul>\n\n  <h3>Step 4 — Details</h3>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li><strong>Initial uses balance</strong> (<code>initialMeterBalance</code>) — required, must be <strong>&gt; 0</strong> (e.g. 500 uses).</li>\n    <li><strong>Notes</strong> — operator context.</li>\n  </ul>\n  <p>Backend creates <code>MeterWallet &#123; mode: Count &#125;</code> and an <strong>InitialGrant</strong> ledger entry.</p>\n  <pre ngNonBindable style=\"margin: 0.75rem 0 1rem; padding: 1rem; background: var(--bs-gray-100, #f5f5f5); border-radius: 0.5rem; font-size: 0.875rem; overflow-x: auto;\"><code>Create Usage-Based license, initial 500 uses\n└─ MeterWallet &#123; mode: Count, balance: 500 &#125;\n   └─ UsageLedger &#123; type: InitialGrant, amount: +500, balanceAfter: 500 &#125;</code></pre>\n\n  <h2>No feature pricing step</h2>\n  <p>\n    Unlike Credit-Based, Usage-Based <strong>ignores</strong> <code>FeatureMeterPricing</code>.\n    <code>ResolveCostAsync</code> sets <code>cost = quantity</code> regardless of feature name.\n    Configure entitlements if you need to restrict which features can be metered.\n  </p>\n\n  <h2>Activation &amp; sessions</h2>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li>Always-online — same activation/session requirements as other online types.</li>\n    <li><strong>No activation seat cap</strong> — unlimited devices may activate the same key.</li>\n    <li>Use global <strong>Activations</strong> / <strong>Sessions</strong> pages for cross-license support views.</li>\n  </ul>\n\n  <h2>Runtime — SDK &amp; API</h2>\n  <pre ngNonBindable style=\"margin: 0.75rem 0 1rem; padding: 1rem; background: var(--bs-gray-100, #f5f5f5); border-radius: 0.5rem; font-size: 0.875rem; overflow-x: auto;\"><code>var client = new LicenseClient(options);\n\nvar result = await client.ValidateAsync();\nif (!result.IsValid) return;\n\n// Each call deducts `quantity` uses (default 1)\nvar consume = await client.ConsumeAsync(\"ApiCall\", quantity: 1);\nif (!consume.Success &amp;&amp; consume.IsInsufficientBalance)\n    ShowTopUpPrompt();\n\nvar balance = await client.GetBalanceAsync(); // remaining uses</code></pre>\n  <p>\n    Read balance without deducting: <code>GET /api/meter/balance?licenseKey=...</code> (API key) or\n    dashboard <code>GET /api/meter/balance/&#123;licenseId&#125;</code>.\n  </p>\n\n  <h3>Worked example</h3>\n  <ol style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li>Create license with initial <strong>500 uses</strong>.</li>\n    <li>Consume any entitled feature ×1 → balance <strong>499</strong>.</li>\n    <li>Consume ×600 in one call → <code>InsufficientBalance</code> (402); balance unchanged at <strong>499</strong>.</li>\n    <li>Consume ×499 → balance <strong>0</strong>.</li>\n    <li>Next consume ×1 → <code>InsufficientBalance</code> (402).</li>\n    <li>Owner grants <strong>+1,000</strong> → balance <strong>1,000</strong>, ledger <code>AdminGrant</code>.</li>\n  </ol>\n  <pre ngNonBindable style=\"margin: 0.75rem 0 1rem; padding: 1rem; background: var(--bs-gray-100, #f5f5f5); border-radius: 0.5rem; font-size: 0.875rem; overflow-x: auto;\"><code>Usage-Based, balance 100, consume quantity 3\n└─ cost = 3 → balance 97\n   └─ UsageLedger &#123; type: Consume, amount: -3, balanceAfter: 97 &#125;</code></pre>\n\n  <h2>Usage tab (license detail)</h2>\n  <p>Open a Usage-Based license → <strong>Usage</strong> tab.</p>\n  <table class=\"doc-table\">\n    <thead><tr><th>Role</th><th>What you see</th></tr></thead>\n    <tbody>\n      <tr><td><strong>Product owner / Admin</strong></td><td>Remaining uses, grant form, full ledger (consumes + grants)</td></tr>\n      <tr><td><strong>End user</strong> (My Licenses)</td><td>Read-only balance; consumption history where exposed</td></tr>\n      <tr><td><strong>Member / Viewer</strong></td><td>Read-only; no grant actions</td></tr>\n    </tbody>\n  </table>\n  <p>\n    The balance card shows “Use mode — each consume deducts one use regardless of feature.”\n    Grants require amount &gt; 0 and are recorded as <strong>AdminGrant</strong> with optional reason and external reference (invoice / PO).\n  </p>\n\n  <h2>Analytics</h2>\n  <p>\n    <code>GET /api/statistics/meter-usage</code> aggregates both metered types:\n    <code>meteredCountLicenseCount</code>, total balance, period consumption, low-balance licenses, daily trend, and top licenses.\n  </p>\n\n  <h2>Consume error codes</h2>\n  <table class=\"doc-table\">\n    <thead><tr><th>ErrorCode</th><th>HTTP</th><th>Meaning</th></tr></thead>\n    <tbody>\n      <tr><td><code>InvalidRequest</code></td><td>400</td><td>Missing required fields</td></tr>\n      <tr><td><code>LicenseNotFound</code></td><td>404</td><td>Unknown license key</td></tr>\n      <tr><td><code>NotMeteredLicense</code></td><td>400</td><td>Not Usage-Based or Credit-Based</td></tr>\n      <tr><td><code>LicenseInvalid</code></td><td>400</td><td>Revoked / expired</td></tr>\n      <tr><td><code>FeatureNotEntitled</code></td><td>403</td><td>Feature blocked by entitlement sets</td></tr>\n      <tr><td><code>InsufficientBalance</code></td><td>402</td><td>Not enough remaining uses</td></tr>\n      <tr><td><code>ConcurrencyConflict</code></td><td>409</td><td>Retry consume</td></tr>\n    </tbody>\n  </table>\n  <p><em>Usage-Based never returns <code>PricingNotConfigured</code></em> — that applies to Credit-Based only.</p>\n\n  <h2>Usage-Based vs Credit-Based</h2>\n  <p>Use this quick guide if you are deciding between the two metered types:</p>\n  <table class=\"doc-table\">\n    <thead><tr><th>Question</th><th>Usage-Based</th><th><a href=\"/credit-based-license\" class=\"doc-inline-link\">Credit-Based</a></th></tr></thead>\n    <tbody>\n      <tr><td>What are you selling?</td><td>A <strong>use counter</strong> (e.g. “500 exports”)</td><td>A <strong>credit wallet</strong> (e.g. “10,000 credits”)</td></tr>\n      <tr><td>Cost per action</td><td><strong>Flat</strong> — 1 use per consume (unless you pass a higher quantity)</td><td><strong>Variable</strong> — each feature has its own token cost</td></tr>\n      <tr><td>Setup before first consume</td><td>Create license with initial use count only</td><td>Set <code>TokenCost</code> per feature, then consume</td></tr>\n      <tr><td>Best analogy</td><td>Punch card — every action uses one punch</td><td>Prepaid wallet — different services cost different amounts</td></tr>\n    </tbody>\n  </table>\n\n  <section class=\"next-steps-section\" style=\"margin-top: 2rem;\">\n    <h2>See also</h2>\n    <div class=\"integration-grid\">\n      <a href=\"/credit-based-license\" class=\"integration-card\"><i class=\"ki-outline ki-wallet\"></i><h4>Credit-Based</h4><p>Token wallet</p></a>\n      <a href=\"/guides/platform/licenses\" class=\"integration-card\"><i class=\"ki-outline ki-key\"></i><h4>Licenses guide</h4><p>Vendor workspace</p></a>\n      <a href=\"/sessions-activations\" class=\"integration-card\"><i class=\"ki-outline ki-chart-line\"></i><h4>Sessions &amp; Activations</h4><p>Monitoring</p></a>\n      <a href=\"/sdk/features-usage\" class=\"integration-card\"><i class=\"ki-outline ki-microsoft\"></i><h4>SDK features &amp; usage</h4><p>ConsumeAsync</p></a>\n    </div>\n  </section>" as const;
