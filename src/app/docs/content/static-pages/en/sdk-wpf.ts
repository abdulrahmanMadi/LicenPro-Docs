export const HTML = "<header class=\"help-page-header\">\r\n    <h1>WPF integration</h1>\r\n    <p class=\"lead\">\r\n      WPF uses the identical <strong>LicenPro</strong> package and APIs as WinForms. There is no separate WPF-specific SDK or <code>ILicenseService</code> shipped by LicenPro — you wrap <code>LicenseClient</code> in your own service registered in DI, keep it alive for the process lifetime, and marshal events to the UI thread as needed.\r\n    </p>\r\n  </header>\r\n\r\n  <h2>1. Application startup</h2>\r\n  <p>Call bootstrap before showing the main window so staged updates and JSON settings load exactly like WinForms.</p>\r\n\r\n  <pre><code>using System.Windows;\r\nusing LicenPro.SDK.AppHosting;\r\n\r\npublic partial class App : Application\r\n{{ '{' }}\r\n    protected override void OnStartup(StartupEventArgs e)\r\n    {{ '{' }}\r\n        SdkBootstrap.OnApplicationStartup();\r\n        base.OnStartup(e);\r\n    {{ '}' }}\r\n{{ '}' }}</code></pre>\r\n\r\n  <h2>2. Async validation without blocking the UI thread</h2>\r\n  <p>\r\n    Construct <code>LicenseClient</code> on a background thread or the UI thread, but always <code>await</code> validation.\r\n    <code>LicenseClient</code> captures <code>SynchronizationContext.Current</code> when created; expiry warnings (<code>LicenseExpiringSoon</code>) are posted back to that context, so constructing the client on the UI thread keeps dialogs safe.\r\n  </p>\r\n\r\n  <h2>3. MVVM-oriented layering</h2>\r\n  <p>Recommended shape:</p>\r\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\r\n    <li><strong>ILicenseService</strong> (your interface) wraps <code>LicenseClient</code>, exposes <code>Task</code>-based validate / try-auto-validate, and forwards <code>SessionDisconnected</code>.</li>\r\n    <li><strong>App-level host</strong> owns service lifetime and calls <code>DisposeAsync</code> on shutdown.</li>\r\n    <li><strong>View models</strong> bind commands to the service; never block the UI thread with synchronous validation APIs.</li>\r\n  </ul>\r\n\r\n  <h2>4. Hardware identity</h2>\r\n  <p>\r\n    The SDK computes a composite hardware fingerprint internally for activation and update checks. You normally do not need a separate “WPF hardware provider”; only supply <code>IHardwareIdentifier</code> in advanced test scenarios.\r\n  </p>\r\n\r\n  <h2>5. Further reading</h2>\r\n  <div class=\"integration-grid\">\r\n    <a href=\"/sdk/dotnet\" class=\"integration-card\">\r\n      <i class=\"ki-outline ki-code\"></i>\r\n      <h4>.NET SDK</h4>\r\n      <p>Options, cache, updates, sessions</p>\r\n    </a>\r\n    <a href=\"/sdk/winforms\" class=\"integration-card\">\r\n      <i class=\"ki-outline ki-mouse\"></i>\r\n      <h4>WinForms reference</h4>\r\n      <p>Parallel lifecycle patterns</p>\r\n    </a>\r\n  </div>" as const;
