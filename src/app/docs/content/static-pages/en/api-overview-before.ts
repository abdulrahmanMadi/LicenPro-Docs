export const HTML = "<article class=\"help-page\">\n  <header class=\"help-page-header\">\n    <h1>REST API overview</h1>\n    <p class=\"lead\">\n      LicenPro exposes an ASP.NET Core API under <code>/api/...</code> on your deployment (cloud:\n      <code>https://licenpro.runasp.net/api</code>). Most management routes require a\n      <strong>Bearer JWT</strong> (dashboard users). Anonymous SDK and server integrations use\n      <strong><code>X-API-KEY</code></strong> on selected routes such as <code>POST /api/Licenses/validate</code>.\n    </p>\n  </header>" as const;
