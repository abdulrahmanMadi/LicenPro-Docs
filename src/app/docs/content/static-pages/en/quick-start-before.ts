export const HTML = "<header class=\"help-page-header\">\r\n    <h1>Quick start</h1>\r\n    <p class=\"lead\">\r\n      LicenPro is a closed loop: you <strong>configure</strong> products and licenses in the <strong>vendor dashboard</strong>,\r\n      you <strong>distribute</strong> signed <code>license.bin</code> files and license keys to customers,\r\n      and your shipped app <strong>enforces</strong> entitlements with the <strong>.NET SDK</strong> (public-key verification plus optional calls to the hosted REST API).\r\n      This page follows that loop in the same order the dashboard and SDK expect—organization first, then product, keys, license, and code.\r\n    </p>\r\n  </header>\r\n\r\n  <div class=\"help-callout info\">\r\n    <i class=\"ki-outline ki-abstract-26\"></i>\r\n    <div>\r\n      <span class=\"callout-title\">Mental model</span>\r\n      <p>\r\n        <strong>Institution</strong> is your tenant boundary—members, invitations, and product ownership.\r\n        <strong>Product</strong> is the anchor for releases, RSA signing, API credentials, entitlements, and every license.\r\n        The <strong>dashboard</strong> signs licenses with a server-side private key; the <strong>SDK</strong> only needs the product <strong>public key</strong>, the customer <strong>license key</strong>, and <code>license.bin</code>.\r\n        Online validation and automation use <code>https://licenpro.runasp.net/api</code> (or your self-hosted API base)—always with the <code>/api</code> suffix.\r\n      </p>\r\n    </div>\r\n  </div>\r\n\r\n  <h2>End-to-end vendor workflow</h2>\r\n  <p>\r\n    The path below matches <a href=\"/guides/platform/overview\" class=\"doc-inline-link\">System overview</a> and the live dashboard at\r\n    <a href=\"https://app.licenpro.tech\" target=\"_blank\" rel=\"noopener noreferrer\">app.licenpro.tech</a>.\r\n    Complete each phase before the next; skipping RSA keys or a software release blocks license issuance.\r\n  </p>\r\n\r\n  <ol style=\"margin: 0.5rem 0 1.5rem 1.25rem;\">\r\n    <li><strong>Account &amp; organization</strong> — sign in and create or join a tenant.</li>\r\n    <li><strong>Product &amp; initial release</strong> — define the app you license and its first version line.</li>\r\n    <li><strong>RSA keys</strong> — generate signing material per product (mandatory before licenses).</li>\r\n    <li><strong>Licenses</strong> — issue keys, export <code>license.bin</code>, set seats, type, and entitlements.</li>\r\n    <li><strong>SDK bootstrap</strong> — point the client at your API, load settings, validate in the app.</li>\r\n    <li><strong>Optional runtime</strong> — activations, sessions, webhooks, and REST automation.</li>\r\n  </ol>\r\n\r\n  <h2>1. Sign in and create an organization</h2>\r\n  <p>\r\n    Register or sign in at <a href=\"https://app.licenpro.tech\" target=\"_blank\" rel=\"noopener noreferrer\">app.licenpro.tech</a>,\r\n    then create or join an <strong>organization</strong>—your tenant for members, products, and licenses.\r\n    If a teammate already invited you, accept that invite instead of creating a second org.\r\n  </p>\r\n  <p>\r\n    Full walkthrough (Identity &amp; Branding wizard, workspace tabs, roles, invitations, API):\r\n    <a href=\"/first-organization\" class=\"doc-inline-link\">Create your first organization</a>.\r\n    Platform reference: <a href=\"/guides/platform/organizations\" class=\"doc-inline-link\">Institutions</a> · <a href=\"/api/organizations\" class=\"doc-inline-link\">Institutions API</a>.\r\n  </p>\r\n\r\n  <h2>2. Create your first product and release</h2>\r\n  <p>\r\n    A <strong>product</strong> is the application you license. Every RSA key pair, API credential, release line, entitlement, and license belongs to one product id.\r\n    From the sidebar open <strong>Products</strong> → <strong>Create Product</strong>, or create from the organization <strong>Products</strong> tab.\r\n  </p>\r\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\r\n    <li><strong>Product name</strong> (required) — unique label in your account.</li>\r\n    <li><strong>Initial version</strong> (required on create) — first <strong>software release</strong> (semver, build number, or custom format). Licenses and update checks target a <code>softwareReleaseId</code>.</li>\r\n    <li><strong>Release channel</strong> — e.g. Stable, Beta, Alpha, RC—so you can separate production and preview builds.</li>\r\n    <li><strong>Product access type</strong> — <strong>Associated</strong> (licenses tied to specific institutions) or <strong>Opened</strong> (any valid license holder).</li>\r\n    <li><strong>Description &amp; image</strong> (optional) — internal documentation and branding.</li>\r\n  </ul>\r\n  <p>\r\n    After save you enter the product workspace (overview, features, entitlements, releases, licenses, users, access matrix, audit).\r\n    Field-level walkthrough: <a href=\"/first-product\" class=\"doc-inline-link\">Create your first product</a> · <a href=\"/guides/platform/products\" class=\"doc-inline-link\">Products</a> · <a href=\"/guides/platform/releases\" class=\"doc-inline-link\">Releases</a>.\r\n  </p>\r\n\r\n  <h2>3. Generate RSA keys (before any license)</h2>\r\n  <p>\r\n    Each product has its own signing identity. From the product workspace open <strong>Settings</strong> / keys and <strong>generate an RSA key pair</strong>.\r\n    LicenPro keeps the <strong>private key</strong> on the server to sign <code>license.bin</code>; your app and SDK embed only the <strong>public key</strong> for verification.\r\n  </p>\r\n  <div class=\"help-callout warning\">\r\n    <i class=\"ki-outline ki-shield\"></i>\r\n    <div>\r\n      <span class=\"callout-title\">Do not ship the private key</span>\r\n      <p>Never paste the private key into customer binaries, config files, or source control. Rotation requires re-issuing licenses signed with the new pair.</p>\r\n    </div>\r\n  </div>\r\n  <p>Full guide: <a href=\"/rsa-keys\" class=\"doc-inline-link\">RSA keys &amp; signing</a> · REST: <code>POST /api/Products/&#123;id&#125;/keys/generate</code> · <code>GET .../keys/public</code>.</p>\r\n\r\n  <h2>4. Copy product API credentials</h2>\r\n  <p>\r\n    Runtime calls from your SDK or backend (for example <code>POST /api/Licenses/validate</code>) use the product <strong>X-API-KEY</strong> header—not your personal login JWT.\r\n    Create or copy this key from the product’s <strong>API / credentials</strong> area in the dashboard (or account pages that list product keys).\r\n    Store it in server config or a vault; only embed it in end-user apps when you intentionally allow anonymous product API calls from the client.\r\n  </p>\r\n  <p>See <a href=\"/guides/platform/overview\" class=\"doc-inline-link\">Credentials at a glance</a> on the system overview and <a href=\"/api/licenses\" class=\"doc-inline-link\">Licenses API</a>.</p>\r\n\r\n  <h2>5. Issue a license and distribute artifacts</h2>\r\n  <p>\r\n    Open the product → <strong>Licenses</strong> → <strong>Create License</strong>. Pick the <strong>software release</strong>, license model (perpetual, trial, subscription, floating, node-locked), seats, expiry, and optional entitlements.\r\n  </p>\r\n  <p>After saving, distribute to customers:</p>\r\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\r\n    <li><code>license.bin</code> — signed payload; download from license details (SDK offline path).</li>\r\n    <li><strong>License key</strong> — human-readable key (e.g. <code>LP-…</code>) for activation UI.</li>\r\n    <li><strong>Public key</strong> — PEM or Base64 export from product keys (SDK verification).</li>\r\n  </ul>\r\n  <p>\r\n    Step-by-step: <a href=\"/first-license\" class=\"doc-inline-link\">Generate your first license</a>.\r\n    Model-specific rules: sidebar <strong>License models</strong> · <a href=\"/guides/platform/licenses\" class=\"doc-inline-link\">Licenses (vendor)</a>.\r\n  </p>\r\n\r\n  <h2>6. Configure the SDK (before validation)</h2>\r\n  <p>Settings resolve in this order:</p>\r\n  <ol style=\"margin: 0.5rem 0 1rem 1.25rem;\">\r\n    <li>Explicit <code>SdkConfiguration.Initialize(...)</code> if you call it in code.</li>\r\n    <li>Otherwise the first file found in the app directory: <code>licenpro.settings.json</code>, then <code>appsettings.json</code> (optional nested <code>LicenPro</code> section).</li>\r\n    <li>Defaults apply if nothing is found—override <code>ServerBaseEndpoint</code> for staging or self-hosting.</li>\r\n  </ol>\r\n\r\n  <pre><code>// Typical explicit bootstrap (any host)\r\nusing LicenPro.Models.Utils;\r\nusing LicenPro.SDK;\r\n\r\nvar settings = new SdkSettings {{ '{' }}\r\n    // Absolute URL ending with /api\r\n    ServerBaseEndpoint = \"https://licenpro.runasp.net/api\"\r\n{{ '}' }};\r\nSdkConfiguration.Initialize(settings);</code></pre>\r\n\r\n  <div class=\"help-callout warning\">\r\n    <i class=\"ki-outline ki-information\"></i>\r\n    <div>\r\n      <span class=\"callout-title\">Common mistake</span>\r\n      <p>Omitting <code>/api</code> or pointing at a marketing domain instead of the API host breaks validation, sessions, and update checks. Match the URL to your deployment.</p>\r\n    </div>\r\n  </div>\r\n\r\n  <h2>7. Desktop bootstrap and validation</h2>\r\n  <p>\r\n    For WinForms, WPF, or other desktop hosts, call <code>SdkBootstrap.OnApplicationStartup()</code> once at process entry (before <code>Application.Run</code>).\r\n    It loads default JSON settings and applies any staged product update from a prior run.\r\n  </p>\r\n\r\n  <pre><code>using LicenPro.SDK.AppHosting;\r\n\r\nApplicationConfiguration.Initialize();\r\nSdkBootstrap.OnApplicationStartup();\r\nApplication.Run(new MainForm());</code></pre>\r\n\r\n  <p>\r\n    Use <code>LicenseClient</code> with <code>LicenseClientOptions</code>: paths to <code>license.bin</code>, public key, license key, optional expected type, optional product id for updates, and expiry-warning tuning.\r\n  Successful validation can populate an on-disk <strong>license cache</strong> for <code>LicenseClient.TryAutoValidateAsync</code> on the next launch.\r\n  </p>\r\n  <p>\r\n    Deep dive: <a href=\"/sdk/dotnet\" class=\"doc-inline-link\">.NET SDK</a> · <a href=\"/sdk/configuration\" class=\"doc-inline-link\">Configuration &amp; bootstrap</a> ·\r\n    <a href=\"/sdk/license-client\" class=\"doc-inline-link\">LicenseClient lifecycle</a> · <a href=\"/sdk/winforms\" class=\"doc-inline-link\">WinForms</a> · <a href=\"/sdk/wpf\" class=\"doc-inline-link\">WPF</a>.\r\n  </p>" as const;
