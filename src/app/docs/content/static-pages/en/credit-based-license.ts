export const HTML = "<header class=\"help-page-header\">\n    <h1>Credit-Based license</h1>\n    <p class=\"lead\">\n      A <strong>Credit-Based</strong> license (<code>MeteredToken</code>, metering mode <code>Token</code>) is\n      <strong>always online</strong>. Customers share one <strong>credit wallet</strong> across all devices on the\n      license key. Each entitled feature deducts credits according to <strong>per-feature token pricing</strong> on\n      the product — not a flat “one use per call” like Usage-Based.\n    </p>\n  </header>\n\n  <div class=\"integration-grid\">\n    <div class=\"integration-card\">\n      <i class=\"ki-outline ki-wallet\"></i>\n      <h4>Shared wallet</h4>\n      <p>One balance per license key — not per device.</p>\n    </div>\n    <div class=\"integration-card\">\n      <i class=\"ki-outline ki-price-tag\"></i>\n      <h4>Variable cost</h4>\n      <p>Each feature has a <code>TokenCost</code> × quantity.</p>\n    </div>\n    <div class=\"integration-card\">\n      <i class=\"ki-outline ki-chart-line-up\"></i>\n      <h4>Usage tab</h4>\n      <p>Balance, ledger, manual top-ups (owners).</p>\n    </div>\n  </div>\n\n  <nav class=\"guide-audience-nav\" aria-label=\"Guide sections\">\n    <span class=\"guide-audience-nav__label\">Jump to:</span>\n    <a href=\"#product-owner-guide\" class=\"guide-audience-nav__link guide-audience-nav__link--owner\">Product owner guide</a>\n    <a href=\"#end-user-guide\" class=\"guide-audience-nav__link guide-audience-nav__link--user\">End user guide</a>\n  </nav>\n\n  <section id=\"product-owner-guide\" class=\"guide-audience-section guide-audience-section--owner\">\n    <h2>Credit-Based license — for product owners</h2>\n    <ul class=\"guide-checklist\">\n      <li>Issue with <strong>initial credit balance</strong> and online validation (always online).</li>\n      <li>Set <strong>per-feature token pricing</strong> via <code>PUT /api/meter/features/&#123;featureId&#125;/pricing</code>.</li>\n      <li>Top up, adjust, set low-balance threshold, and reconcile from the license <strong>Usage</strong> tab or meter API.</li>\n      <li>Inspect ledger for consumes, grants, and idempotent replays.</li>\n      <li>No activation seat cap — many devices share one wallet.</li>\n    </ul>\n    <p>Detailed wizard steps, pricing, and operator API routes are documented below.</p>\n  </section>\n\n  <section id=\"end-user-guide\" class=\"guide-audience-section guide-audience-section--user\">\n    <h2>Credit-Based license — for end users</h2>\n    <p>\n      Your organization shares a license key with a <strong>credit wallet</strong>. The app stays valid even at\n      zero credits, but <strong>billable features fail</strong> until credits are added.\n    </p>\n    <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n      <li>Activate once — validation does not deduct credits.</li>\n      <li>Each paid action (export, render, API call) spends credits per feature pricing.</li>\n      <li><strong>Insufficient balance</strong> — contact your admin to purchase more credits.</li>\n      <li>Check remaining balance in-app if the integrator exposed <code>GetBalanceAsync</code>.</li>\n    </ul>\n  </section>\n\n  <h2>When to choose Credit-Based</h2>\n  <p>\n    Choose <strong>Credit-Based</strong> when you sell a <strong>pre-paid pool of credits</strong> and different\n    features in your product should cost <strong>different amounts</strong>. The customer starts with a balance\n    (for example 10,000 credits). Each time they run a billable action, your app calls\n    <code>ConsumeAsync</code> and the server deducts credits based on that feature’s configured price.\n  </p>\n  <p>\n    Think of it like a prepaid phone plan with variable call rates: one minute to country A costs 2 credits,\n    one minute to country B costs 5 credits — same wallet, different prices per action.\n  </p>\n\n  <h3>Good fits</h3>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li><strong>API or SaaS with tiered actions</strong> — “10,000 API credits per month”; a simple read costs 1 credit, a heavy export costs 25.</li>\n    <li><strong>Document / OCR / AI products</strong> — “5,000 pages included”; OCR costs 10 credits per page, summary costs 40.</li>\n    <li><strong>Creative or compute tools</strong> — “1,000 render credits”; preview is cheap, 4K export is expensive.</li>\n    <li><strong>Top-up sales</strong> — customer buys another 5,000 credits when the wallet is low (manual grant from the Usage tab today).</li>\n  </ul>\n\n  <h3>How it behaves in LicenPro</h3>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li>Dashboard label: <strong>Credit-Based</strong>. API enum: <code>MeteredToken</code>.</li>\n    <li>Balance is shown in <strong>credits</strong> (shared across all devices on the license key).</li>\n    <li>You must set a <strong>token cost per feature</strong> before consume works (via metering API; UI coming later).</li>\n    <li>Example: feature <code>ExportPdf</code> priced at 25 → one consume deducts <strong>25 credits</strong>, not 1.</li>\n  </ul>\n\n  <h3>When to use Usage-Based instead</h3>\n  <p>\n    If every billable action should cost <strong>exactly one use</strong> — “500 exports included”, “1,000 API calls” —\n    and you do <strong>not</strong> need different prices per feature, use\n    <a href=\"/usage-based-license\" class=\"doc-inline-link\">Usage-Based</a> (<code>MeteredCount</code>). It is simpler: no feature pricing step.\n  </p>\n\n  <h2>At a glance</h2>\n  <table class=\"doc-table\">\n    <thead>\n      <tr><th>Property</th><th>Value</th></tr>\n    </thead>\n    <tbody>\n      <tr><td>Backend enum</td><td><code>LicenseType.MeteredToken</code> (value <code>6</code>)</td></tr>\n      <tr><td>Dashboard label</td><td><strong>Credit-Based</strong></td></tr>\n      <tr><td>Metering mode</td><td><code>MeteringMode.Token</code></td></tr>\n      <tr><td>Balance unit</td><td><strong>credits</strong></td></tr>\n      <tr><td>Cost per consume</td><td><code>FeatureMeterPricing.TokenCost × quantity</code></td></tr>\n      <tr><td>Activation mode</td><td><strong>Online only</strong> — offline is not available</td></tr>\n      <tr><td>Seat limit</td><td>None — any number of devices may activate; the wallet is the limit</td></tr>\n    </tbody>\n  </table>\n\n  <h2>Key invariant</h2>\n  <p>\n    Balance is <strong>only</strong> checked and deducted in <code>POST /api/meter/consume</code>.\n    License validation, activation, and session connect <strong>never</strong> look at the balance.\n    A Credit-Based license can activate at zero credits; your app must call <code>ConsumeAsync</code> when a\n    billable feature runs and handle <code>InsufficientBalance</code> (HTTP 402).\n  </p>\n\n  <h2>Create a Credit-Based license (dashboard)</h2>\n  <p>\n    Open <strong>Licenses</strong> → <strong>Generate License</strong>. Four steps:\n    <strong>Activation</strong> → <strong>License Type</strong> → <strong>Basic Info</strong> → <strong>Details</strong>.\n  </p>\n\n  <h3>Step 1 — Activation</h3>\n  <p>Select <strong>Online</strong>. Credit-Based is an always-online type — the wizard forces online mode and hides metered cards if Offline is selected.</p>\n\n  <h3>Step 2 — License type</h3>\n  <p>Select the <strong>Credit-Based</strong> card (“Variable cost per feature”). Subtitle explains the shared credit pool and manual top-ups.</p>\n  <figure class=\"doc-figure doc-figure-card\">\n    <button type=\"button\" class=\"doc-figure-view-link\" data-doc-image-lightbox=\"/assets/docs/platform-create-license-step-license-type.png\" data-doc-image-alt=\"License Type step with Credit-Based selected\" aria-label=\"View License Type step screenshot full size\">\n      <img src=\"assets/docs/platform-create-license-step-license-type.png\" alt=\"License Type step with Credit-Based selected\" loading=\"lazy\" decoding=\"async\" />\n    </button>\n    <figcaption class=\"doc-figure-caption\">License Type — Credit-Based (MeteredToken).</figcaption>\n  </figure>\n\n  <h3>Step 3 — Basic info</h3>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li><strong>Product</strong> and <strong>Software release</strong> (required).</li>\n    <li><strong>Issued to</strong> — end user assigned to the release (required for non-perpetual types).</li>\n    <li><strong>License name</strong> — internal label in lists and audit.</li>\n    <li>Optional <strong>Entitlement set</strong> — when set, only entitled features can be consumed.</li>\n  </ul>\n\n  <h3>Step 4 — Details</h3>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li><strong>Initial credits balance</strong> (<code>initialMeterBalance</code>) — required, must be <strong>&gt; 0</strong> (default in the form is often 10,000).</li>\n    <li><strong>Notes</strong> — operator context (invoice, SKU, support ticket).</li>\n  </ul>\n  <p>On save, the backend creates a <code>MeterWallet</code> with mode <code>Token</code> and writes an <strong>InitialGrant</strong> ledger row when balance &gt; 0.</p>\n  <pre ngNonBindable style=\"margin: 0.75rem 0 1rem; padding: 1rem; background: var(--bs-gray-100, #f5f5f5); border-radius: 0.5rem; font-size: 0.875rem; overflow-x: auto;\"><code>Create Credit-Based license, initial 10,000\n└─ MeterWallet &#123; mode: Token, balance: 10000 &#125;\n   └─ UsageLedger &#123; type: InitialGrant, amount: +10000, balanceAfter: 10000 &#125;</code></pre>\n\n  <h2>Configure per-feature pricing (Credit-Based only)</h2>\n  <p>\n    Before clients consume a feature, set its token cost on the product.\n    API: <code>PUT /api/meter/features/&#123;featureId&#125;/pricing</code> (product owner / admin).\n  </p>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li><code>TokenCost</code> must be <strong>&gt; 0</strong>.</li>\n    <li>Previous active pricing rows are deactivated; history is preserved.</li>\n    <li>Consuming a feature with <strong>no active pricing</strong> returns <code>PricingNotConfigured</code> (HTTP 422).</li>\n  </ul>\n  <p><em>Note:</em> A dedicated pricing UI in the dashboard is planned; today pricing is set via API.</p>\n\n  <h2>Activation &amp; sessions</h2>\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li>Always-online — activation and sessions are required like Floating.</li>\n    <li><strong>No activation seat cap</strong> — <code>EnforceSeatLimitOnActivation</code> is false for metered types; many devices may share one key.</li>\n    <li>Activations and sessions appear in the dashboard for support visibility (optional attribution on consume via <code>hardwareId</code> / <code>activationId</code>).</li>\n  </ul>\n  <p>See <a href=\"/guides/platform/activations\" class=\"doc-inline-link\">Activations</a> and <a href=\"/guides/platform/sessions\" class=\"doc-inline-link\">Sessions</a> platform guides.</p>\n\n  <h2>Runtime — SDK &amp; API</h2>\n  <p>Typical client flow:</p>\n  <pre ngNonBindable style=\"margin: 0.75rem 0 1rem; padding: 1rem; background: var(--bs-gray-100, #f5f5f5); border-radius: 0.5rem; font-size: 0.875rem; overflow-x: auto;\"><code>var client = new LicenseClient(options);\n\n// 1. Validate (always online — does NOT check credit balance)\nvar result = await client.ValidateAsync();\nif (!result.IsValid) return;\n\n// 2. Before running a billable feature — deduct credits\nvar consume = await client.ConsumeAsync(\"ExportPdf\", quantity: 1);\nif (!consume.Success &amp;&amp; consume.IsInsufficientBalance)\n    ShowTopUpPrompt();\n\n// 3. Optional — show remaining credits\nvar balance = await client.GetBalanceAsync();</code></pre>\n  <p>\n    <code>ConsumeAsync</code> calls <code>POST /api/meter/consume</code> with product <code>X-API-KEY</code>.\n    Pass an <strong>idempotency key</strong> to make retries safe — replays return the prior result without double-charging.\n  </p>\n\n  <h3>Consume request</h3>\n  <table class=\"doc-table\">\n    <thead><tr><th>Field</th><th>Required</th><th>Notes</th></tr></thead>\n    <tbody>\n      <tr><td><code>licenseKey</code></td><td>Yes</td><td>Customer license key</td></tr>\n      <tr><td><code>featureName</code></td><td>Yes</td><td>Must be entitled; must have active pricing</td></tr>\n      <tr><td><code>idempotencyKey</code></td><td>Yes</td><td>Reuse on retry of the same logical operation</td></tr>\n      <tr><td><code>quantity</code></td><td>No (default 1)</td><td>Cost = <code>TokenCost × quantity</code></td></tr>\n      <tr><td><code>hardwareId</code></td><td>No</td><td>Auto-detected by SDK when omitted</td></tr>\n      <tr><td><code>activationId</code></td><td>No</td><td>Attribute usage to a device row</td></tr>\n    </tbody>\n  </table>\n\n  <h3>Worked example</h3>\n  <ol style=\"margin: 0.5rem 0 1rem 1.25rem;\">\n    <li>Create license with initial <strong>10,000 credits</strong>.</li>\n    <li>Price feature <code>\"render\"</code> at <strong>TokenCost 40</strong>.</li>\n    <li>SDK consumes <code>render</code> ×1 → cost 40 → balance <strong>9,960</strong>.</li>\n    <li>Retry with same <code>idempotencyKey</code> → replay, balance still <strong>9,960</strong>.</li>\n    <li>Consume unpriced feature → <code>PricingNotConfigured</code> (422).</li>\n    <li>Owner grants <strong>+5,000</strong> → balance <strong>14,960</strong>, ledger <code>AdminGrant</code>.</li>\n  </ol>\n\n  <h2>Usage tab (license detail)</h2>\n  <p>Open a Credit-Based license → <strong>Usage</strong> tab.</p>\n  <table class=\"doc-table\">\n    <thead><tr><th>Role</th><th>What you see</th></tr></thead>\n    <tbody>\n      <tr><td><strong>Product owner / Admin</strong></td><td>Remaining balance, <strong>Grant balance</strong> form (amount, reason, external reference), full usage ledger</td></tr>\n      <tr><td><strong>Assigned end user</strong> (My Licenses → detail)</td><td>Read-only balance card; ledger without grant actions</td></tr>\n      <tr><td><strong>Member / Viewer</strong></td><td>No grant UI; read-only where permitted</td></tr>\n    </tbody>\n  </table>\n  <p>Ledger entry types: <strong>InitialGrant</strong>, <strong>Consume</strong> (negative amount), <strong>AdminGrant</strong>, <strong>Adjust</strong>.</p>\n  <p>Top-ups are <strong>manual only</strong> — there is no payment gateway integration in the current release.</p>\n\n  <h2>Analytics</h2>\n  <p>\n    Institution analytics includes a metered usage section from\n    <code>GET /api/statistics/meter-usage</code>: metered license counts, total balance remaining,\n    consumed/granted in the period, low-balance alerts, daily consumption trend, top features, and top licenses.\n  </p>\n\n  <h2>Consume error codes</h2>\n  <table class=\"doc-table\">\n    <thead><tr><th>ErrorCode</th><th>HTTP</th><th>Meaning</th></tr></thead>\n    <tbody>\n      <tr><td><code>InvalidRequest</code></td><td>400</td><td>Missing key, feature, or idempotency key</td></tr>\n      <tr><td><code>LicenseNotFound</code></td><td>404</td><td>No license for the key</td></tr>\n      <tr><td><code>NotMeteredLicense</code></td><td>400</td><td>Wrong license type</td></tr>\n      <tr><td><code>LicenseInvalid</code></td><td>400</td><td>Status is not Valid</td></tr>\n      <tr><td><code>FeatureNotEntitled</code></td><td>403</td><td>Feature not in entitlement sets (when sets are configured)</td></tr>\n      <tr><td><code>WalletNotFound</code></td><td>400</td><td>Wallet missing</td></tr>\n      <tr><td><code>PricingNotConfigured</code></td><td>422</td><td>No active <code>TokenCost</code> for this feature</td></tr>\n      <tr><td><code>InsufficientBalance</code></td><td>402</td><td><code>balance &lt; cost</code></td></tr>\n      <tr><td><code>ConcurrencyConflict</code></td><td>409</td><td>Retry the consume call</td></tr>\n    </tbody>\n  </table>\n\n  <h2>Credit-Based vs Usage-Based</h2>\n  <p>Use this quick guide if you are deciding between the two metered types:</p>\n  <table class=\"doc-table\">\n    <thead><tr><th>Question</th><th>Credit-Based</th><th><a href=\"/usage-based-license\" class=\"doc-inline-link\">Usage-Based</a></th></tr></thead>\n    <tbody>\n      <tr><td>What are you selling?</td><td>A <strong>credit wallet</strong> (e.g. “10,000 credits”)</td><td>A <strong>number of actions</strong> (e.g. “500 exports”)</td></tr>\n      <tr><td>Cost per action</td><td><strong>Varies by feature</strong> — you set each feature’s token price</td><td><strong>Always the same</strong> — usually 1 use per consume</td></tr>\n      <tr><td>Setup before first consume</td><td>Configure <code>TokenCost</code> per feature</td><td>Nothing extra — just create the license with an initial use count</td></tr>\n      <tr><td>Best analogy</td><td>Prepaid credits with different prices per service</td><td>“Includes 500 uses” — each button click counts as one</td></tr>\n    </tbody>\n  </table>\n\n  <section class=\"next-steps-section\" style=\"margin-top: 2rem;\">\n    <h2>See also</h2>\n    <div class=\"integration-grid\">\n      <a href=\"/usage-based-license\" class=\"integration-card\"><i class=\"ki-outline ki-chart-simple\"></i><h4>Usage-Based</h4><p>Flat use counter</p></a>\n      <a href=\"/guides/platform/licenses\" class=\"integration-card\"><i class=\"ki-outline ki-key\"></i><h4>Licenses guide</h4><p>Vendor workspace</p></a>\n      <a href=\"/first-license\" class=\"integration-card\"><i class=\"ki-outline ki-plus-square\"></i><h4>First license</h4><p>Wizard walkthrough</p></a>\n      <a href=\"/sdk/features-usage\" class=\"integration-card\"><i class=\"ki-outline ki-microsoft\"></i><h4>SDK features &amp; usage</h4><p>ConsumeAsync</p></a>\n    </div>\n  </section>" as const;
