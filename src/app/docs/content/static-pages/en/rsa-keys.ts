export const HTML = "<header class=\"help-page-header\">\r\n    <h1>RSA keys, signing &amp; API credentials</h1>\r\n    <p class=\"lead\">\r\n      LicenPro uses two different kinds of keys: <strong>RSA signing keys</strong> (per product, for <code>license.bin</code>)\r\n      and <strong>account API keys</strong> (for your automation scripts with scoped REST access). Customer apps and the SDK\r\n      need the product <strong>public key</strong> and optionally the product <code>X-API-KEY</code>—never your RSA private key or operator password.\r\n    </p>\r\n  </header>\r\n\r\n  <div class=\"integration-grid\">\r\n    <div class=\"integration-card\">\r\n      <i class=\"ki-outline ki-lock-2\"></i>\r\n      <h4>RSA private key</h4>\r\n      <p>Server-only; signs every license file.</p>\r\n    </div>\r\n    <div class=\"integration-card\">\r\n      <i class=\"ki-outline ki-shield-tick\"></i>\r\n      <h4>RSA public key</h4>\r\n      <p>Embed in apps; SDK verifies signatures.</p>\r\n    </div>\r\n    <div class=\"integration-card\">\r\n      <i class=\"ki-outline ki-key\"></i>\r\n      <h4>Account API keys</h4>\r\n      <p>JWT-backed REST automation with scopes.</p>\r\n    </div>\r\n  </div>\r\n\r\n  <h2 style=\"margin-top: 3rem;\">Part 1 — RSA signing keys (per product)</h2>\r\n  <p>\r\n    Each product has its own RSA key pair. When you issue or download a license, LicenPro signs the payload with the\r\n    <strong>private key</strong> stored on the server. Your application embeds only the matching <strong>public key</strong>\r\n    so <code>LicenseClient</code> can prove the file was issued for that product and was not tampered with.\r\n  </p>\r\n\r\n  <h3>Dashboard: generate and download</h3>\r\n  <ol style=\"margin: 0.5rem 0 1rem 1.25rem;\">\r\n    <li>Open the product → <strong>Settings</strong> (<code>/dashboard/products/.../settings</code>).</li>\r\n    <li>Scroll to <strong>License signing keys (RSA)</strong>.</li>\r\n    <li>Click <strong>Generate keys</strong> if none exist, or <strong>Regenerate</strong> when rotating (see warning below).</li>\r\n    <li>Click <strong>Download public key</strong> (PEM) for operators or copy Base64 material for SDK config.</li>\r\n  </ol>\r\n\r\n  <figure class=\"doc-figure doc-figure-card\">\r\n    <button type=\"button\" class=\"doc-figure-view-link\" data-doc-image-lightbox=\"/assets/docs/platform-product-settings-rsa-keys.png\" data-doc-image-alt=\"Product Settings: License signing keys RSA section\" aria-label=\"View screenshot full size\">\r\n      <img src=\"assets/docs/platform-product-settings-rsa-keys.png\" alt=\"Product Settings: License signing keys RSA section\" loading=\"lazy\" decoding=\"async\" />\r\n    </button>\r\n    <figcaption class=\"doc-figure-caption\">Click image to open full-resolution preview.</figcaption>\r\n  </figure>\r\n\r\n  <div class=\"help-callout warning\">\r\n    <i class=\"ki-outline ki-shield-cross\"></i>\r\n    <div>\r\n      <span class=\"callout-title\">Never ship the private key</span>\r\n      <p>The private key never leaves LicenPro. Do not paste it into apps, config files, or customer deliverables.</p>\r\n    </div>\r\n  </div>\r\n\r\n  <h3>How signing fits validation</h3>\r\n  <p>\r\n    <code>LicenseClient.ValidateAsync</code> loads your public key (via <code>LicenseClientOptions.PublicKey</code>)\r\n    and verifies the RSA signature on <code>license.bin</code>. Wrong product file, edited bytes, or stale key after rotation\r\n    yields <code>LicenseValidationStatus.SignatureMismatch</code>.\r\n  </p>\r\n\r\n  <pre><code>using LicenPro.SDK;\r\n\r\nawait using var client = new LicenseClient(new LicenseClientOptions &lbrace;\r\n    LicenseFilePath = \"license.bin\",\r\n    PublicKey = \"MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8A...\",\r\n    LicenseKey = \"LP-....\"\r\n&rbrace;);</code></pre>\r\n\r\n  <h3>REST API (operator JWT)</h3>\r\n  <p>Authorized product access required:</p>\r\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\r\n    <li><code>POST /api/Products/&#123;id&#125;/keys/generate</code> — create or regenerate pair.</li>\r\n    <li><code>GET /api/Products/&#123;id&#125;/keys/status</code> — whether keys exist and metadata.</li>\r\n    <li><code>GET /api/Products/&#123;id&#125;/keys/public</code> — JSON public key for integrations.</li>\r\n    <li><code>GET /api/Products/&#123;id&#125;/keys/public/download</code> — PEM file download.</li>\r\n  </ul>\r\n\r\n  <h3>Key rotation</h3>\r\n  <div class=\"help-callout warning\">\r\n    <i class=\"ki-outline ki-information\"></i>\r\n    <div>\r\n      <span class=\"callout-title\">Rotation impact</span>\r\n      <p>Regenerating RSA keys invalidates every <code>license.bin</code> signed with the old private key. Plan: publish app builds with the new public key, re-issue licenses, and stagger customer upgrades.</p>\r\n    </div>\r\n  </div>\r\n\r\n  <h3>Related: derive-key endpoint</h3>\r\n  <p>\r\n    <code>POST /api/security/derive-key</code> (anonymous) derives material from <code>licenseKey</code> and salt for protected license file flows.\r\n    The SDK coordinates this with <code>LicenseFileProtector</code> patterns where the customer key participates in decryption.\r\n  </p>\r\n\r\n  <h2 style=\"margin-top: 4rem;\">Part 2 — Account API keys (REST automation)</h2>\r\n  <p>\r\n    Separate from RSA and separate from the per-product <code>X-API-KEY</code> used on anonymous validation routes.\r\n    <strong>Account API keys</strong> authenticate automation (CI, billing jobs, internal tools) to management APIs using scoped permissions.\r\n    Manage them under <strong>Settings</strong> → <strong>API keys</strong> (<code>/dashboard/settings</code>, Developer &amp; account section).\r\n  </p>\r\n\r\n  <figure class=\"doc-figure doc-figure-card\">\r\n    <button type=\"button\" class=\"doc-figure-view-link\" data-doc-image-lightbox=\"/assets/docs/account-api-keys.png\" data-doc-image-alt=\"Account Settings API keys page with Generate API Key button\" aria-label=\"View API keys page screenshot full size\">\r\n      <img src=\"assets/docs/account-api-keys.png\" alt=\"Account Settings API keys page with Generate API Key button\" loading=\"lazy\" decoding=\"async\" />\r\n    </button>\r\n    <figcaption class=\"doc-figure-caption\">Click image to open full-resolution preview.</figcaption>\r\n  </figure>\r\n\r\n  <h3>Generate API Key modal</h3>\r\n  <p>Click <strong>Generate API Key</strong> and complete the form:</p>\r\n\r\n  <figure class=\"doc-figure doc-figure-card\">\r\n    <button type=\"button\" class=\"doc-figure-view-link\" data-doc-image-lightbox=\"/assets/docs/account-generate-api-key.png\" data-doc-image-alt=\"Generate API Key modal: name, description, expiration, and permission scopes\" aria-label=\"View Generate API Key modal screenshot full size\">\r\n      <img src=\"assets/docs/account-generate-api-key.png\" alt=\"Generate API Key modal: name, description, expiration, and permission scopes\" loading=\"lazy\" decoding=\"async\" />\r\n    </button>\r\n    <figcaption class=\"doc-figure-caption\">Click image to open full-resolution preview.</figcaption>\r\n  </figure>\r\n\r\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\r\n    <li><strong>Name</strong> (required) — e.g. <em>CI/CD Pipeline</em>.</li>\r\n    <li><strong>Description</strong> (optional) — rich text explaining purpose.</li>\r\n    <li><strong>Expiration date</strong> (optional) — leave empty for no expiration.</li>\r\n    <li><strong>Scopes</strong> — least privilege; typical pairs:\r\n      <ul style=\"margin: 0.5rem 0; padding-left: 1.25rem;\">\r\n        <li>Read / Write Licenses</li>\r\n        <li>Read / Write Activations</li>\r\n        <li>Read / Write Products</li>\r\n        <li>Read / Write Releases</li>\r\n      </ul>\r\n    </li>\r\n  </ul>\r\n\r\n  <div class=\"help-callout warning\">\r\n    <i class=\"ki-outline ki-shield\"></i>\r\n    <div>\r\n      <span class=\"callout-title\">One-time display</span>\r\n      <p>After <strong>Generate Key</strong>, the secret is shown once. Copy it to a vault immediately; revoked keys cannot be recovered.</p>\r\n    </div>\r\n  </div>\r\n\r\n  <h2>Credentials cheat sheet</h2>\r\n  <ul style=\"margin: 0.5rem 0 1rem 1.25rem;\">\r\n    <li><strong>Operator JWT</strong> — browser session after dashboard login; management routes.</li>\r\n    <li><strong>Account API key</strong> — scoped automation from Settings → API keys.</li>\r\n    <li><strong>Product X-API-KEY</strong> — per-product header for anonymous product routes (e.g. validate).</li>\r\n    <li><strong>RSA public key</strong> — verify <code>license.bin</code> in the SDK.</li>\r\n    <li><strong>RSA private key</strong> — never distributed; signs on server only.</li>\r\n  </ul>\r\n  <p>See also <a href=\"/guides/platform/overview\" class=\"doc-inline-link\">Credentials at a glance</a> on the system overview.</p>\r\n\r\n  <section class=\"next-steps-section\" style=\"margin-top: 3rem;\">\r\n    <h2>Continue learning</h2>\r\n    <div class=\"integration-grid\">\r\n      <a href=\"/first-product\" class=\"integration-card\">\r\n        <i class=\"ki-outline ki-abstract-26\"></i>\r\n        <h4>First product</h4>\r\n        <p>Where RSA keys live</p>\r\n      </a>\r\n      <a href=\"/first-license\" class=\"integration-card\">\r\n        <i class=\"ki-outline ki-key\"></i>\r\n        <h4>First license</h4>\r\n        <p>Download license.bin</p>\r\n      </a>\r\n      <a href=\"/api/security\" class=\"integration-card\">\r\n        <i class=\"ki-outline ki-lock\"></i>\r\n        <h4>Security API</h4>\r\n        <p>API keys &amp; derive-key</p>\r\n      </a>\r\n      <a href=\"/sdk/dotnet\" class=\"integration-card\">\r\n        <i class=\"ki-outline ki-code\"></i>\r\n        <h4>.NET SDK</h4>\r\n        <p>Validation stack</p>\r\n      </a>\r\n    </div>\r\n  </section>" as const;
